- name: Update and upgrade apt packages
  apt:
    upgrade: yes
    update_cache: yes
    cache_valid_time: 86400 #One day

- name: Install default packages
  apt:
    name:
      - curl
      - vim
      - htop
      - wget
    state: latest